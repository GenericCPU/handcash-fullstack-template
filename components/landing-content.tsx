"use client"

/** Public homepage hero — no HandCash business wallet branding (name/logo). */
export function LandingContent() {
  return (
    <div className="mb-6 text-center sm:mb-8">
      <p className="mx-auto max-w-md text-[15px] leading-relaxed text-muted-foreground">
        Sign in below with HandCash or Google to use profile, wallet, friends, and inventory features.
      </p>
    </div>
  )
}
